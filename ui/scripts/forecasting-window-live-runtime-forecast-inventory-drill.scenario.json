{
  "baseUrl": "http://127.0.0.1:5173",
  "requireSession": true,
  "viewport": {
    "width": 1400,
    "height": 1200
  },
  "steps": [
    {
      "type": "goto",
      "url": "/mcp-ui/forge-window?windowKey=forecastingCubeBuilder"
    },
    {
      "type": "waitForDomContains",
      "text": "Quick chart",
      "timeoutMs": 60000
    },
    {
      "type": "clickRole",
      "role": "button",
      "name": "Close"
    },
    {
      "type": "fillSelector",
      "selector": "input[type=\"date\"]",
      "index": 0,
      "value": "2026-06-10",
      "timeoutMs": 60000
    },
    {
      "type": "fillSelector",
      "selector": "input[type=\"date\"]",
      "index": 1,
      "value": "2026-06-12",
      "timeoutMs": 60000
    },
    {
      "type": "clickSelector",
      "selector": ".forge-report-builder__chart-action-button--quick",
      "force": true
    },
    {
      "type": "clickText",
      "text": "Overview · Avails by Date and Channel",
      "timeoutMs": 60000
    },
    {
      "type": "waitForEval",
      "expression": "(() => { const root = document.querySelector('.forge-report-builder__runtime-preview'); if (!root) return false; const text = root.innerText || root.textContent || ''; const rows = root.querySelectorAll('tbody tr').length; return rows > 0 && !text.includes('Refreshing authored runtime') && !text.includes('No data.'); })()",
      "timeoutMs": 60000
    },
    {
      "type": "waitForEval",
      "expression": "(() => { const labels = Array.from(document.querySelectorAll('.forge-report-builder__runtime-preview th')).map((node) => (node.innerText || node.textContent || '').trim()); return labels.includes('Channel') && labels.includes('Actions'); })()",
      "timeoutMs": 60000
    },
    {
      "type": "waitForEval",
      "expression": "document.querySelectorAll('.forge-report-builder__runtime-preview .forge-dashboard-row-action').length >= 1",
      "timeoutMs": 60000
    },
    {
      "type": "clickSelectorContains",
      "selector": ".forge-report-builder__runtime-preview .forge-dashboard-row-action",
      "text": "Drill to Publisher",
      "index": 0,
      "timeoutMs": 60000
    },
    {
      "type": "waitForEval",
      "expression": "Array.from(document.querySelectorAll('.forge-report-builder__runtime-preview .forge-report-runtime-refinement-chip')).some((node) => { const text = (node.innerText || node.textContent || '').trim(); const value = (text.split('=')[1] || '').trim(); return text.includes('Drill to Publisher =') && /[A-Za-z]/.test(value); })",
      "timeoutMs": 60000
    },
    {
      "type": "waitForEval",
      "expression": "(() => { const labels = Array.from(document.querySelectorAll('.forge-report-builder__runtime-preview th')).map((node) => (node.innerText || node.textContent || '').trim()); return labels.includes('Publisher') && !labels.includes('Channel'); })()",
      "timeoutMs": 60000
    },
    {
      "type": "clickSelectorContains",
      "selector": ".forge-report-builder__runtime-preview .forge-dashboard-row-action",
      "text": "Drill to Site Type",
      "index": 0,
      "timeoutMs": 60000
    },
    {
      "type": "waitForEval",
      "expression": "Array.from(document.querySelectorAll('.forge-report-builder__runtime-preview .forge-report-runtime-refinement-chip')).some((node) => { const text = (node.innerText || node.textContent || '').trim(); const value = (text.split('=')[1] || '').trim(); return text.includes('Drill to Site Type =') && /[A-Za-z]/.test(value); })",
      "timeoutMs": 60000
    },
    {
      "type": "waitForEval",
      "expression": "(() => { const labels = Array.from(document.querySelectorAll('.forge-report-builder__runtime-preview th')).map((node) => (node.innerText || node.textContent || '').trim()); return labels.includes('Site Type') && !labels.includes('Publisher'); })()",
      "timeoutMs": 60000
    },
    {
      "type": "assertConsoleNotContains",
      "text": "Maximum update depth exceeded"
    },
    {
      "type": "assertConsoleNotContains",
      "text": "Failed to fetch data"
    },
    {
      "type": "screenshot",
      "file": "forecasting-window-live-runtime-forecast-inventory-drill-proof.png",
      "fullPage": true
    }
  ]
}
