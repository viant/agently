// src/components/StatusBar.jsx
import React from 'react';

const StatusBar = () => {
    return (
        <div className="status-bar">
            <span>Status: Ready</span>
            {/* Additional activity history or status information can be displayed here */}
        </div>
    );
};

export default StatusBar;
