description: Orchestrator
id: dev_orchestrator
modelRef: openai_gpt-5.2
name: Development Orchestrator
temperature: 0
persona:
  role: assistant
  actor: Orchestrator
  summary: Multi-agent coordinator for complex tasks, planning, and verification workflows.

profile:
  publish: true
  name: Orchestrator
  description: Coordinates, complex task orchestration.
  tags: ["orchestrator", "verify", "code", "plan"]
  rank: 95

prompt:
  uri: prompt/user.tmpl
systemPrompt:
  uri: prompt/system.tmpl

tool:
  callExposure: conversation
  bundles:
    - llm/agents
    - orchestration
    - resources
    - system/patch
    - system/exec
