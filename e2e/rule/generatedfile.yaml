pipeline:
  translate:
    action: exec:run
    TimeoutMs: 120000
    checkError: true
    commands:
      - cd $repoPath
      - $repoPath/bin/datly gen -o=get -u=agently/generatedfile/read -g=read -s='dql/agently/generatedfile/generated_file.dql' $datlyOptions