service:
  endpoint: agentlyAPI
  uri: /v1/workspace/workflow
  method: GET
  post:
    uri: /v1/api/workflow/run

selectors:
  data: data

uniqueKey:
  - field: name

filterSet:
  - name: default
    default: true
    template:
      - id: name
        label: Filter workflows...
        operator: contains

quickFilterSet: default
