dataSourceRef: usage
uniqueKey:
  - field: model

selectors:
  data: perModel

parameters:
  - name: model
    to: :query
    from: 'usage:'
    location: conversationID

selectionMode: single
autoSelect: true