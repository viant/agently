service:
  endpoint: agentlyAPI
  # Datly list endpoint
  uri: /v1/api/agently/scheduler/schedule/
  method: GET

paging:
  size: 10

selectors:
  data: data

on:
  - event: onFetch
    handler: schedule.onFetchSchedules

uniqueKey:
  - field: id
