columns:
  - id: name
    name: Name
    width: 220
    enforceColumnSize: true
    tooltip: OAuth profile name
  - id: clientID
    name: Client Id
    width: 260
    enforceColumnSize: true
    tooltip: OAuth client identifier

# Toolbar tailored for picker – only refresh button
toolbar:
  items:
    - id: refresh
      icon: refresh
      align: left
