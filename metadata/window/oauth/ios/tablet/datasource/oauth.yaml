service:
  endpoint: agentlyAPI
  uri: /v1/workspace/oauth


selectors:
  data: data

uniqueKey:
  - field: name

selectionMode: single

filterSet:
  - name: default
    default: true
    template:
      - id: name
        label: Filter oauth...
        operator: contains

quickFilterSet: default
